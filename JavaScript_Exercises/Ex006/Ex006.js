// base x altura

let base = Number(window.prompt('Digite a base do retangulo: '))
let altura = Number(window.prompt('Digite a altura do retangulo: '))

let area = base * altura

console.log(`A area do retangulo é ${area}`)

