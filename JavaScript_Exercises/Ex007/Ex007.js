let base = Number(window.prompt('Digite a base do triangulo: '))
let altura = Number(window.prompt('Digite a altura do triangulo: '))

let area =  (base * altura) / 2

window.alert(`A area do triangulo é de ${area}`)

console.log(`A area do triangulo é de ${area}`)

